<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the exports directory is shared, and only measurements are written there.
         Nothing else the app holds is reachable through this provider. -->
    <cache-path name="exports" path="exports/" />
</paths>
